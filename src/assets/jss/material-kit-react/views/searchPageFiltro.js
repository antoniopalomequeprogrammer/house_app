import { unstable_createMuiStrictModeTheme } from "@material-ui/core";
import { container, title } from "assets/jss/material-kit-react.js";

const searchPageFiltroStyle = {
  filtro: {
    maxWidth: '90%',
    width: '100%',
    margin: '0 auto'
  },
};

export default searchPageFiltroStyle;
